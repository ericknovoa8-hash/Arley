server:
  port: 8087

spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/taller
    username: root
    password:  
    driver-class-name: org.mariadb.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update 
    show-sql: true
    properties:
      hibernate:
        format_sql: true